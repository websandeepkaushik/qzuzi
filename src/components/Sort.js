import React from 'react';
import styled from 'styled-components';

const Sort = () => {
    return(
        <SortContainer>Sort component</SortContainer>
    )
}

export default Sort;

export const SortContainer = styled.div`
    padding: 10px;
`;